<div class="card my-4">
    <h5 class="card-header">Search</h5>
    <div class="card-body">
        <div class="input-group">
            <form action="{{ path('widget_search_search') }}" method="POST" style="display:flex;">
                <input type="text" class="form-control" name="texte" placeholder="Search for...">
                <span class="input-group-append">
                    <button class="btn btn-secondary" type="submit">Go!</button>
                </span>
            </form>
        </div>
    </div>
</div>
